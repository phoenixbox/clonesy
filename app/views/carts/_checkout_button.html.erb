<% if current_user %>
  <%= link_to 'Checkout', store_checkout_path(current_store), class: 'btn' %>
<% else %>
  <a href="#checkout_modal" role="button" class="btn" data-toggle="modal">Checkout</a>
  <%= render 'checkout_modal' %>
<% end %>
