<tr>
  <td><%= link_to "Order ##{order.id}", uber_order_path(order.id) %></td>
  <td><%= order.created_at.to_s(:short) %></td>
  <td><%= number_to_currency(order.total) %></td>
  <td><%= order.status %></td>
</tr>
