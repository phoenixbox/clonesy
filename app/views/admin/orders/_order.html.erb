<tr>
  <td><%= link_to "#{t('order.self')} ##{order.id}", store_admin_order_path(current_store, order.id) %></td>
  <td><%= order.created_at.to_s(:short) %></td>
  <td><%= order.status %></td>
  <td><%= number_to_currency(order.total) %></td>
  <td><%= number_to_currency(order.total(current_store)) %></td>
</tr>
