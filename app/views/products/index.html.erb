<div class="product-index">
  <section class="row">
    <%= render 'category_dropdown', locals: { categories: @categories } %>

    <%= render 'product_search', locals: { search_value: params[:search] } %>
  </section>

  <br/>

  <div id="product_listing" class="row-fluid">
    <%= render @products %>
  </div>
</div>

